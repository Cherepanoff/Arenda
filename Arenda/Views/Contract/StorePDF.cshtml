@model IEnumerable<Arenda.Models.PdfFile>
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<body>
    <h2>Договор склада</h2>
    @foreach (var m in Model)
    {
        @if (m.Pdfstore != null)
        {
            <a class="btn" href="@m.Pdfstore" target="_blank">@m.Pdfstore</a>
        }
    }
    <form asp-controller="Contract" asp-action="UploadStorePDF" asp-route-id=@TempData["ID"]
          enctype="multipart/form-data" method="post">
        <input type="file" name="STOREPDF" />
        <button type="submit" class="btn">Загрузить договор</button>
    </form>
</body>